That not actually an implementation. That's an example of
using "Observer" pattern in Swing library.

`Jbutton` is a subject that has method fod adding `ActionListener`

Every `ActionListener` is an observer